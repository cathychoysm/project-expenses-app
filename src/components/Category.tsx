import { Text, VStack } from "@chakra-ui/react";

export default function Category() {
  return (
    <VStack
      width="full"
      minH={800}
      bg="yellow.300"
      padding={30}
      borderRadius={10}
      gap={20}
    >
      <Text>Coming soon🤩</Text>
    </VStack>
  );
}
